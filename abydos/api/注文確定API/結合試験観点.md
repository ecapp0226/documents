# 注文確定API バックエンド結合試験観点

## 1. 認証（実JWT＋Security FilterChain通し確認）

| No | 観点 | 期待結果 |
|----|------|----------|
| 1-1 | Authorizationヘッダーなしでリクエスト | 401 API_ERR003 |
| 1-2 | 無効なJWTトークンでリクエスト | 401 API_ERR003 |
| 1-3 | 有効期限切れのJWTトークンでリクエスト | 401 API_ERR003 |
| 1-4 | 有効なJWTでリクエスト | JWTからuser_idが正しくデコードされ処理続行 |

## 2. バリデーション（単体テスト未カバー分）

| No | 観点 | 期待結果 |
|----|------|----------|
| 2-1 | リクエストボディなし | 400 API_ERR001 |
| 2-2 | products がちょうど100件 | バリデーション通過し正常処理 |
| 2-3 | product_id が文字列（型不正） | 400エラー |
| 2-4 | quantity が 1（最小有効値） | バリデーション通過 |

## 3. 注文可否チェック（複数商品・実DB動作）

| No | 観点 | 期待結果 |
|----|------|----------|
| 3-1 | 複数商品が全て存在する場合の正常パス | チェック通過 |
| 3-2 | 全件が存在しない商品ID | 400 API_ORDER_ERR001 |
| 3-3 | 同一product_idが複数行で指定＋実DBでBatchGetItem | 集約後に正しく取得される |
| 3-4 | 複数商品中1件が購入不可（status="S" or "D"） | 400 API_ORDER_ERR002 |
| 3-5 | 在庫数 = 0 の商品を1個注文 | 400 API_ORDER_ERR003 |

## 4. 採番（実DynamoDB アトミックインクリメント）

| No | 観点 | 期待結果 |
|----|------|----------|
| 4-1 | order_id 採番 | sequencesテーブルのcurrent_valueがインクリメントされ、返却値がordersに使用される |
| 4-2 | detail_id 採番（商品3件） | current_valueが3加算され、連番(n+1, n+2, n+3)が各明細に割り当て |
| 4-3 | 初回採番（sequence_nameのレコードが未存在） | ADDにより0起点で採番される |
| 4-4 | 連続注文時のorder_id | 重複なく連番が発行される |

## 5. トランザクション（実TransactWriteItems＋DB登録結果検証）

| No | 観点 | 期待結果 |
|----|------|----------|
| 5-1 | ordersテーブル登録 | user_id(PK), order_id(SK), created_at, detail_ids が正しく登録 |
| 5-2 | order_detailsテーブル登録 | 商品件数分のレコード（order_id, detail_id, product_id, product_name, shop_id, price, order_num, delivery_status="PR", created_at） |
| 5-3 | productsテーブル在庫更新 | 各商品のstockが注文数量分減少 |
| 5-4 | product_name/priceがスナップショットとして保存 | productsテーブルの値がorder_detailsにコピーされる |
| 5-5 | delivery_statusの初期値 | "PR"（処理中）で登録 |
| 5-6 | ConditionExpression失敗時のロールバック | ordersテーブルにレコードなし |
| 5-7 | ConditionExpression失敗時のロールバック | order_detailsテーブルにレコードなし |
| 5-8 | ConditionExpression失敗時のロールバック | products在庫が元のまま |
| 5-9 | 同一商品の重複を事前集約してからトランザクション実行 | TransactWriteItems内で同一キーへの重複操作が発生しない |

## 6. 同時実行・競合

| No | 観点 | 期待結果 |
|----|------|----------|
| 6-1 | 2ユーザーが同一商品（在庫=1）を同時注文 | 一方は成功、もう一方はConditionExpression失敗 → API_ORDER_ERR003 |
| 6-2 | 2ユーザーが異なる商品を同時注文 | 両方とも成功、order_idが重複しない |
| 6-3 | チェック通過後〜トランザクション実行前に他ユーザーが在庫を0にした | ConditionExpressionで検出しロールバック |

## 7. 異常系・システムエラー

| No | 観点 | 期待結果 |
|----|------|----------|
| 7-1 | DynamoDB接続エラー（タイムアウト等） | 500 API_ERR999 |
| 7-2 | 想定外の例外発生 | 500 API_ERR999 |

## 8. レスポンス形式検証

| No | 観点 | 期待結果 |
|----|------|----------|
| 8-1 | 正常完了時のHTTPステータス | 200 OK、ボディなし |
| 8-2 | エラー時のレスポンスボディ（API_ORDER_ERR001） | `{ "error_code": "API_ORDER_ERR001", "message": "product not found." }` |
| 8-3 | エラー時のレスポンスボディ（API_ORDER_ERR002） | `{ "error_code": "API_ORDER_ERR002", "message": "product unavailable." }` |
| 8-4 | エラー時のレスポンスボディ（API_ORDER_ERR003） | `{ "error_code": "API_ORDER_ERR003", "message": "out of stock." }` |
| 8-5 | エラー時のレスポンスボディ（API_ERR999） | `{ "error_code": "API_ERR999", "message": "server error." }` |

## 9. データ整合性（E2E検証）

| No | 観点 | 期待結果 |
|----|------|----------|
| 9-1 | 1商品1個の注文後のDB状態 | orders: 1レコード、order_details: 1レコード、products: stock -1 |
| 9-2 | 3商品の注文後のDB状態 | orders: 1レコード(detail_ids=3件)、order_details: 3レコード、products: 各stock減少 |
| 9-3 | 同一商品が2行(qty=2,qty=3)の注文後 | orders: 1レコード、order_details: 1レコード(order_num=5)、products: stock -5 |
| 9-4 | created_atの値 | 現在時刻(UnixTimestampミリ秒)が妥当な範囲であること |
| 9-5 | 注文後に注文履歴取得APIで確認 | 登録内容と注文履歴取得結果が一致 |
